<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="farociuldin.cursoandroid.foundit.com">
    <!-- Permissão internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Galeria/Camera -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="farociuldin.cursoandroid.foundit.com.activity.DetalhesProdutoActivity"
            android:parentActivityName="farociuldin.cursoandroid.foundit.com.activity.AnunciosActivity">
        </activity>
        <activity android:name="farociuldin.cursoandroid.foundit.com.activity.CadastrarAnuncioActivity"
            android:parentActivityName="farociuldin.cursoandroid.foundit.com.activity.MeusAnunciosActivity"/>
        <activity
            android:name="farociuldin.cursoandroid.foundit.com.activity.MeusAnunciosActivity"
            android:label="@string/title_activity_meus_anuncios"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName="farociuldin.cursoandroid.foundit.com.activity.AnunciosActivity"/>
        <activity android:name="farociuldin.cursoandroid.foundit.com.activity.AnunciosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </activity>
        <activity android:name="farociuldin.cursoandroid.foundit.com.activity.CadastroActivity" />
    </application>

</manifest>